# Architecture

The stable boundary is `raw observation -> normalized observation -> account -> score -> workflow`. Raw values remain available for auditability while canonical organization and domain keys support aggregation.

Detailed phase evidence and implementation notes are maintained in [docs/knowledge/README.md](knowledge/README.md). Update the corresponding phase note whenever a pipeline phase completes.